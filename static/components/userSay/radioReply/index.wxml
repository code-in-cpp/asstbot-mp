  <view>
    <view class="wrapper word-text right-block">
      <block wx:if="{{content.value}}">
        <view class="outgoing right-item" >
          <copyable-text text="{{content.value}}"/>
        </view>
      </block>
    </view>
    <view class="right-block word-text" wx:if="{{content.imageUrl}}">
      <image bindlongpress="previewImage" class="image" mode="aspectFit" src="{{content.imageUrl}}"></image>
    </view>
  </view>
